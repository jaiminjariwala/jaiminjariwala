const Philosophy = () => {
  return (
    <section className="h-screen px-[6vw] text-white flex items-center">
      <h1 className="max-w-[60vw] text-left text-[clamp(2rem,6vw,5.5rem)] font-light tracking-[0.05px] leading-tight">
        Less, but better.
      </h1>
    </section>
  );
};

export default Philosophy;
